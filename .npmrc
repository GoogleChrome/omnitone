# Pin the public npm registry.
#
# Without this, `npm install` run behind a corporate or mirroring proxy writes
# that proxy's hostnames into package-lock.json's "resolved" fields. Those URLs
# are unreachable for everyone else, which breaks `npm ci` in CI and for
# external contributors.
registry=https://registry.npmjs.org/
